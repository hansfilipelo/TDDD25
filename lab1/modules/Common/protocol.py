
# Requests methods
READ = "r"
WRITE = "w"

# Responses methods
RESULT = "a"
ERROR = "e"


# will take in the type of message and the arguments
# and make it to a sendable json message.
def create_msg(type, args=[]):
    return ""


# Will take in a json msg and a

def read_msg(msg):
    return json.load(msg)










